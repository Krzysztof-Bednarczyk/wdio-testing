name: $(date:yyyyMMdd)$(rev:.r)
pr:
  branches:
    include:
    - main
    - develop
    - release/*
    - hotfix/*
    - feature/*
    - pr/*
resources:
- repo: self
variables:
  debug: 'true'
stages:
  - template: stages/test.yml
    parameters:
      stageName: 'Testing'
      stageDisplayName: 'Building and Testing Stage'